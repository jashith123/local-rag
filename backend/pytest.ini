[pytest]
testpaths = tests
# Quiet the noise from model downloads and third-party deprecations so a real
# failure is the only thing on screen.
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
addopts = -q
