# API
fastapi==0.139.2
uvicorn==0.51.0
python-multipart==0.0.32
python-dotenv==1.2.2

# Document processing
pypdf==6.14.2

# Embeddings (local, CPU — pulls in torch, ~2 GB)
sentence-transformers==5.6.1

# Vector database (embedded by default; set QDRANT_URL for a server)
qdrant-client==1.18.0

# Answer generation (needs ANTHROPIC_API_KEY in backend/.env)
anthropic==0.122.0
